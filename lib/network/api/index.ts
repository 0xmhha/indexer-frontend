/**
 * API Functions
 * 백엔드와의 async 통신 함수
 *
 * 이 모듈은 훅에서 사용되는 저수준 API 함수들을 제공합니다.
 * 모든 함수는 async로 동작하며, 파서를 통해 Raw → Domain 변환을 수행합니다.
 *
 * TODO: 향후 구현 예정
 * - fetchBlock(hash: string): Promise<Block>
 * - fetchBlocks(options: BlocksQueryOptions): Promise<PaginatedResult<Block>>
 * - fetchTransaction(hash: string): Promise<Transaction>
 * - fetchAddress(address: string): Promise<Address>
 */

// Placeholder exports - 향후 API 함수 구현 시 추가
// export { fetchBlock, fetchBlocks } from './blocks'
// export { fetchTransaction, fetchTransactions } from './transactions'
// export { fetchAddress } from './addresses'
